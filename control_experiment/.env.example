# Control experiment only (run from control_experiment/, e.g. port 3001).
# Use a different Apps Script deployment URL than the main experiment (root .env.local NEXT_PUBLIC_WEBHOOK_URL).
# CONTROL_SHEETS_WEBHOOK_URL=
# NEXT_PUBLIC_CONTROL_SHEETS_WEBHOOK_URL=
